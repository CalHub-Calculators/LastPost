<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LastPost</title>
    <!--FavIcon-->
     <link rel="icon" type="image/x-icon" href="\icons\last_logo.png">
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        :root { --primary-color: #000000; --accent-color: #0052FF; }
        body { font-family: 'Inter', sans-serif; background-color: #ffffff; color: #1a1a1a; }
        .navbar-brand { font-weight: 800; letter-spacing: -1px; font-size: 1.5rem; }
        .hero-section { padding: 100px 0 60px 0; background: radial-gradient(circle at top right, #f8f9ff, #ffffff); }
        .card { border: none; border-radius: 16px; transition: all 0.3s ease; }
        .card:hover { transform: translateY(-8px); }
        .post-title { font-weight: 700; line-height: 1.3; text-decoration: none; color: #1a1a1a; }
        .post-title:hover { color: var(--accent-color); }
        .badge-category { background: #f0f2f5; color: #555; font-weight: 600; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px; padding: 6px 12px; border-radius: 100px; }
        .btn-premium { background: var(--primary-color); color: white; border-radius: 100px; padding: 12px 28px; font-weight: 600; transition: 0.3s; }
        .btn-premium:hover { background: var(--accent-color); color: white; }
    </style>
</head><head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'FounderStartup | Premium Tech Insights' %></title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        :root { --primary-color: #000000; --accent-color: #0052FF; }
        body { font-family: 'Inter', sans-serif; background-color: #ffffff; color: #1a1a1a; }
        .navbar-brand { font-weight: 800; letter-spacing: -1px; font-size: 1.5rem; }
        .hero-section { padding: 100px 0 60px 0; background: radial-gradient(circle at top right, #f8f9ff, #ffffff); }
        .card { border: none; border-radius: 16px; transition: all 0.3s ease; }
        .card:hover { transform: translateY(-8px); }
        .post-title { font-weight: 700; line-height: 1.3; text-decoration: none; color: #1a1a1a; }
        .post-title:hover { color: var(--accent-color); }
        .badge-category { background: #f0f2f5; color: #555; font-weight: 600; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px; padding: 6px 12px; border-radius: 100px; }
        .btn-premium { background: var(--primary-color); color: white; border-radius: 100px; padding: 12px 28px; font-weight: 600; transition: 0.3s; }
        .btn-premium:hover { background: var(--accent-color); color: white; }

        /* Navbar / dropdown hover */
        .navbar-nav .nav-link {
            font-weight: 500;
            font-size: 0.95rem;
        }
        .nav-item.dropdown:hover .dropdown-menu {
            display: block;
            margin-top: 0;
        }
        .dropdown-menu {
            border-radius: 12px;
            box-shadow: 0 12px 30px rgba(0,0,0,0.08);
            border: none;
        }
        .dropdown-item {
            font-size: 0.9rem;
        }
    </style>
</head>