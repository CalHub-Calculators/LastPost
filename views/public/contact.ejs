<!DOCTYPE html>
<html lang="en">
<%- include('../partials/header') %>
<body>
    <%- include('../partials/main-nav') %>

    <main class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h1 class="display-5 fw-bold mb-3 text-center">Contact</h1>
                <p class="text-muted fs-5 mb-4 text-center">
                    Have a question, partnership idea, or feedback? Reach out below.
                </p>

                <% if (query && query.sent) { %>
                    <div class="alert alert-success">Thanks, your message has been sent.</div>
                <% } %>

                <form class="card border-0 shadow-sm p-4" style="border-radius: 16px;"
                      method="POST" action="/contact">
                    <div class="mb-3">
                        <label class="form-label">Name</label>
                        <input class="form-control" type="text" name="name" placeholder="Your name" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <input class="form-control" type="email" name="email" placeholder="you@example.com" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Message</label>
                        <textarea class="form-control" rows="4" name="message" placeholder="How can we help?" required></textarea>
                    </div>
                    <button type="submit" class="btn btn-dark px-4">Send message</button>
                </form>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>